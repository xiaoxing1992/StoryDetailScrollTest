pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven {
            url 'https://nexus.skyplatanus.com/repository/maven-public/'
            credentials {
                username "$skymavenUser"
                password "$skymavenPassword"
            }
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url 'https://nexus.skyplatanus.com/repository/maven-public/'
            credentials {
                username "$skymavenUser"
                password "$skymavenPassword"
            }
        }
    }
}
rootProject.name = "StoryDetailScrollTest"
include ':app'
